describe('Game of Life', function () {
    'use strict';

    var assert = require('assert'),
        g = require('../src/game');

    it('is 42', function () {
        assert.strictEqual(g.foo, 42);
    });

    it('is bla description', function () {

        assert.equals(cell.hasANeieighbor(), false);


    })

});
